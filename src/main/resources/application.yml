spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/estapar_parking
    username: seu_usuario_postgres
    password: sua_senha_postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    format-sql: true